# 获取 arbitration 控制权。相同完整 binding 的 acquire 重试必须幂等返回同一 owner_epoch。
# 未知 source、冲突 binding、非法 TTL 或无法确认的状态必须 fail closed。
string source_type
string session_id
string run_id
string resource_id
string source_id
uint64 requested_ttl_ms
---
bool granted
string reason_code
uint64 owner_epoch
# 机器人本机 steady clock 绝对纳秒，不是 Unix epoch。
uint64 expires_at_steady_ns
