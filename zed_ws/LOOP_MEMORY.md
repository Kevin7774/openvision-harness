
- (up 11940s, run loop-001) ⑭ 改一条理解：我原来打算「让人把黄积木推到 x=0.48 就有解」，这是错的。probe_min_grasp_z.py 的文档记着 08-13 实测：x=0.588 和 x=0.48 在抓取高度(桌面+13mm)**都无解**，而两者在桌面+133mm 都有 0.00mm 残差的解 ⇒ 卡点不是横向可达(BAND_X)，是**高度＋顶抓朝向**这一对。所以推积木救不了，别再提这个方案。真嫌疑犯是接近轴容差：摇操唯一一次真夹住(grip=124)的位姿接近角是 **59.5°**，而 grasp_block/agent_loop 默认 tol=45 会把那个位姿本身判成无解 —— 也就是说这 30 多次「无解」里至少有一部分是判据比现实严。probe_free_at_tol.py 另记：tol=70 沿解支下探到桌面+43mm 就吃紧(残差 0.00→10.18mm、dev 0.583 顶着 0.6 的帽子)，吃紧的是帽子。下一步只问一个问题：tol=70 且不限 max_dev 时抓取高度到底有没有解(agent_loop act probe --tol 70)。

- (up 12334s, run loop-001) ⑬⑭ 预测被推翻，记下来：x=0.589 −0.187 在 **tol=70 且不限 max_dev（自由全局搜索）** 下，抓取高度(桌面+13mm)**仍然无解**(331s / 8 轮全空)；同一点预抓取(+133mm) 12s 出 0.00mm 残差解。所以「tol=45 判据过严」这个假设**救不了这个点** —— 桌面+13mm 那一层在 x=0.589 是真的到不了，是运动学不是判据。tol 45→70 这个改动本身仍然对(45 会把摇操那个 59.5° 的真值位姿判成无解)，但它不是这次的解释。剩下唯一未测的量：x=0.480 在 tol=70 下有没有抓取高度解(08-13 那次「无解」是 tol=45 量的，已作废)。已在跑。

- (up 12436s, run loop-001) ⑭ **今天最硬的一对实测**（都是 tol=70、自由 IK、指尖中点、桌面 0.8108）：
  · x=0.589 y=-0.187 抓取位(桌面+13mm) **无解** 331s/8轮全空；预抓取(+133mm) 12s / 0.00mm
  · x=0.480 y=-0.187 抓取位(桌面+13mm) **有解 0.00mm 残差 24s**；预抓取 2s / 0.00mm
两条推论：
① 「x=0.48 抓取高度也无解、所以推积木救不了」这句**错了**，它是 tol=45 量出来的。tol=70 下 x=0.48 是能抓的 ⇒ **请人把黄积木从 0.589 往近侧推 ~11cm 到 x≈0.48 就能抓**，这是当前唯一确定可行的动作。
②  的上界在抓取高度那一层**被实测支持**：0.589(>0.56) 不行、0.480(<0.56) 行。它错在被当成所有高度的判据（预抓取 +133mm 那层 0.589 有解），不错在数值。
所以正确说法既不是「横向不可达」也不是「只是高度」，是**抓取高度那层的横向上界比预抓取层紧**，两层要分别问。判据一律 act probe --tol 70。

- (up 13049s, run loop-002) 第一次观察：原图确认黄色积木位于桌面，base_link约(0.522,-0.179)，检测框正确，作业区无人，双臂未遮挡ZED。

- (up 13508s, run loop-002) 第二次观察对账：solve耗时407秒；预抓取(0.522,-0.179,0.9438)残差0mm，但抓取高度z=0.8238无同解支安全解；机器人未动，黄色积木仍在(0.5217,-0.1794)。禁止继续pregrasp/descend，也不放宽门禁。

- (up 14736s, run loop-002) ⑨⑭ **两个 session 的「矛盾」不存在：积木在 13:21~13:52 之间被挪近了 67mm。** 证据是同一条感知路径(zed_pointcloud_bbox)、同一头位姿(39.0/0.0)、depth_frac 都 1.0，但像素从 (693,265) → (703,307)、面积 738→867 —— 物体真的动了。所以 loop-001 的「x=0.589 抓取高度无解(331s)」和 loop-002 的「x=0.522 有解(22s)」都对，只是测的是两个位置。⇒ 教训：**跨 session 引用坐标必须带时间戳**，「当前抓取点」这四个字在共享机器上没有意义。现在唯一卡点不是可达性，是下降链：x=0.522 自由解 22s 出，但 pregrasp→grasp 限 max_dev=0.6 无解 407s ⇒ `--clear 0.12` 那个世界 z 垂直下降 12cm 的固定形状太僵。

- (up 14841s, run loop-002) 诊断订正：当前黄块(0.522,-0.179,0.8238)并非物理无解。act solve在先选预抓取解后，以max_dev=0.6约束同支下降，406秒返回无解；但act probe tol=70取消该解支限制后，预抓取7秒、抓取22秒均为0mm残差。根因是预抓取解支选择/成对搜索不完整，不是ZED坐标或70度姿态。安全修法应搜索兼容的q_pre/q_grasp对或抓取位反向上解，不能删除0.6rad与地板/碰撞门禁。
