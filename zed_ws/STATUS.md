# 现状 — 2026-08-14 CST（**最新，先读这一节**）

> ## 🔀 2026-08-14 夜：路线切到**纯视觉像素伺服**，`scripts/servo.py` 已落地
>
> 操作者定案走纯视觉。计划全文 `docs/12_pure_vision_servo_plan.md`；实现是**一个新文件**
> `scripts/servo.py`（`measure` / `step` / `demo`），其余全部复用已有件：
> `pad_offset` 的 `_setup/_grab/_pad_px/_grip_safe/_project`＋`grasp_block` 的
> `hsv_blobs/plan_path/collide`＋关节限位。
>
> **为什么换**：几何链是三个串乘的标定常量（相机外参 · 桌高 · 工具模型），前两个今天才刚
> 各错 37mm / 190mm 改过，第三个（外参）**还剩 ~100mm 没定位**。伺服量的是
> 「夹垫像素 → 积木像素」这个**差**，常量偏移在差分里为零 —— 这三个数一个都不进链。
>
> **进度**：`python3 servo.py demo` **6/6 通过**（离线，不碰机器人）：4 张历史帧全检出两片
> 夹垫 · `J⁺J=I` · `e=0⇒Δq=0` · 单步夹逼 · 安全层确实接在 `step` 的路上 · 停滞判据。
> 同一张帧上离线量到真实误差信号：夹垫 (540,341) vs 黄积木 (702,314) ⇒ **|e|=164px**。
>
> 🔴 **伺服只有提议权。** `plan_path` 碰撞 / `_grip_safe` / 指尖地板 / 关节限位一条都没放宽；
> 被否决时 `step` 返回 4 并把理由写进 `experiments/servo/steps.jsonl`，那是设计不是故障。
>
> ### ✅ P1 增益矩阵已量到（`experiments/servo/J_zed_20260814-193432.json`，条件数 **2.7**）
>
> | 关节 | du/dq | dv/dq | ‖·‖ px/rad |
> |---|---|---|---|
> | `arm_2` | −695.5 | +346.4 | 777 |
> | `arm_4` | +314.2 | −506.7 | 596 |
> | `arm_6` | −100.9 | +450.7 | 462 |
>
> **它有一个独立的外部对账**：08-14 `pad_offset.swing` 那次（另一天、另一个目的、30° 不是
> 8°）推出 arm_6 约 485 px/rad，这次量到 **462** —— 差 5%。这是本项目第一次有两个独立
> 测量互相吻合到 10% 以内。起点位姿 tcp `[0.374,-0.128,1.008]`（桌面 +122mm），6 个
> ±8° 扰动全过闸门，**`home_safely` 不必跑**（可达性是当前构型的属性，这个构型就是合格的）。
>
> 顺带量到一个没预期的数：`measure` 六次往返回到 q0 之后，夹垫复现到 **0.3px**
> （477.6,274.6 → 477.3,274.5）。整条「回位＋检出」链是亚像素可重复的。
>
> ### ✅ P2 闭环第一步：**预测和现实吻合到 5%**
>
> ```
> e = [101.6 33.2]  |e| = 106.9px      Δq = arm_2 −5.68° arm_4 −1.93° arm_6 +4.31°
> PREDICT 夹垫 Δ[50.8 16.6]   ACTUAL Δ[47.6 17.6]   同向 是   增益比 0.95
> |e| 106.9 → 56.3px（预测降 50%，实际降 47%）
> ```
>
> 对比一下这个数量级：几何链上一次的账是「瞄准只差 2.2mm 却爪合空了」（真误差 190mm）。
> 像素链第一步就把 `--predict` 变成了一个**有数、可伪的**量，而且它对上了。
>
> ### ✅ P2 收敛判据达成：四步 106.9 → 9.1px，预测全程同向
>
> | 步 | \|e\| 前 → 后 | 降幅 | 增益比（实际/预测） |
> |---|---|---|---|
> | 1 | 106.9 → 56.3 | 47% | 0.95 |
> | 2 | 57.6 → 30.2 | 48% | 0.96 |
> | 3 | 30.2 → 16.1 | 47% | 0.94 |
> | 4 | 16.0 → 9.1 | 43% | 0.87 |
>
> `damp=0.5` 预测每步降 50%，实测 43~48%，**几何收敛**。增益比在 3.5 倍步长范围内稳定在
> 0.94~0.96 ⇒ 线性模型在整段上都成立，不只在工作点附近。第 4 步掉到 0.87 是像素噪声开始
> 占比（复现性 0.3px + 质心噪声 ~1px，在 9px 误差上就是 10% 量级）—— 这就是 ZED 段的
> 精度地板，不是模型错。
>
> **标度**：`arm_2` 8°(0.1396rad) 时 tcp 走 59.0mm、夹垫走 777px/rad ⇒ **1.84 px/mm**，
> 所以 9.1px 残差 ≈ **4.9mm**。几何链错 190mm，像素链剩 4.9mm，差 38 倍。
>
> **看过原图**（`experiments/servo/20260814-193646-step-after/rgb.png`）：两片夹垫**正跨在**
> 黄积木两侧，夹垫中点落在积木上。这是一个货真价实的预抓取对齐，而链里**没有相机外参、
> 没有桌高、没有工具模型**。
>
> ⚠️ **别过度解读**：这是**图像平面**的二维对齐。沿相机射线的**深度完全没被这个误差观测到**
> —— 二维 e=0 不等于夹爪到了积木那儿。这正是计划里要「相机接力」的原因：最后那段必须换
> 视角（腕部相机/D405），靠的是「下一台相机里看得见」而不是坐标变换。ZED 段到此为止。
>
> **手臂现在停在哪**（多个 session 共用这台机器）：伺服收敛后的位姿，两片夹垫跨在黄积木
> 两侧、爪**开着**，头 pitch +39° / yaw 0。这个构型过了全部闸门（起点 tcp 桌面 +122mm），
> 可以直接接着跑 `servo.py step`。要挪走别用 `xr1.py home`（单次 1.8rad 上限会 MotionRefused）。
>
> 待办：靠近后换腕部相机（靶心退化成固定准星）。D405/D455 都还没装，P3+ 等硬件；
> **P1/P2 不等硬件，已做完**。

> ## ✅ 2026-08-14 晚：190mm 指尖误差**已定案并改了常量**；桌高连带从 0.8108 降到 **0.790**
>
> 下面所有旧节里的 `TCP_TO_TIP=48.5mm`、`TABLE_TOP/TABLE_Z=0.8108`、`GRASP_TIP_Z=0.8238`
> **全部作废**，凡是引用这三个数得出的结论（尤其是可达性 / 「桌子超出可达范围」/
> 「x=0.48 有解、x=0.589 无解」）都要重算。
>
> | 常量 | 旧 | 新 | 依据 |
> |---|---|---|---|
> | `TCP_TO_TIP` | 0.0485 | **0.168** | 操作者卷尺 法兰→夹垫中心 230mm，减 tcp 在法兰外的 62mm |
> | `GRASP_TIP_Z` | 0.8238 | **0.8001** | 真值取自 `truth_samples.jsonl` **第 4 条**（`grip=124`，唯一落在夹持带内的），不是第 1 条 |
> | `TABLE_TOP` | 0.8108 | **0.790** | 上面那条推出的带 [0.770,0.790] 取**上沿**（估高只拒动，估低会压桌，代价不对称） |
>
> **两件事同时错，且同向：** ①真值锚点引的是一条 `grip=839`（**完全张开**）的假记录 ——
> 筛选判据 `GRIP_HOLD_BAND_MM=(40,400)` 就写在同一个文件里，只是没拿来筛数据；
> ②指尖常量小了 3.5 倍。旧值「和 ZED 深度 0.8128 差 2.0mm，两链互证」是假记录造的巧合
> （用真记录 + 旧常量差 28mm）。ZED 那侧独立自证偏高：`--vista` 6 参拟合解出与夹爪几何
> 无关的世界系偏移 `b=[-39.5,+83.5,-43.5]mm`，z 分量 ⇒ ZED 的桌高应在 ~0.773。
>
> **结构问题的判据不经过相机：** 只转 `arm_6` −30°、tcp 走 94.5mm，夹垫在像素里走
> 252.2px 而 tcp 走 140.7px（比值 **1.79**）。常量世界偏移必须给出恰好 **1.0**
> ⇒ 偏移刚性挂在腕上 ⇒ `TIP_CENTER` 真错。差分消掉了任何常量外参误差。
>
> ⚠️ **还剩一个独立的 ~100mm 世界系误差（大概率相机外参），不在 `TIP_CENTER` 里，也不该折进去。**
> 归 `correction.json` / `USE_HANDEYE` 那条线，目前仍关着。
>
> 复现 / 自检：`python3 scripts/pad_offset.py --demo`（含 `_check_truth_anchor()`，钉住
> 「只用夹持带内的记录」+「桌高必须落在锚点推出的带里」）、`--vista`（8 帧联合拟合，
> tool z 张角仍 <45° 所以它现在老实打**不判定**）、`--swing`（差分实验）。
> 另有 5 处 `--table-z` / `TABLE_Z` 字面副本已改成引用 `grasp_block.TABLE_TOP`
> （`head_fov_check` `plane_now` `base_clearance` `reach_ik_map`，`ik_probe` 因循环 import 只能同步字面量）。
> ⚠️ 还有 5 个 recovery 脚本硬写 `G.TABLE_TOP = G.TABLE_Z = 0.824`（`retry_edge_grasp`
> `second_plan_probe` `global_edge_grasp` `recover_pregrasp` `recover_retry`）—— 那是往**高**
> 估（保守方向、不会压桌），先没动，但它们现在和权威值差 34mm。
>
> ---
>
> ## ⚠️（已被上面覆盖）卡点已换：不是解支绑定，是 **FK 声称的手指位置比真手指偏了约 190mm**
>
> ### 机器状态（loop-003 收工时）
>
> | 项 | 值 |
> |---|---|
> | 右臂 | `park_open` `[0,-1.0,0,-0.4,0,0,0]`，指尖 (0.112, −0.888, 0.885)，桌面半平面外且高于桌面 |
> | 右爪 | 839 = 全开 | 
> | 左臂 / 颈 | 未动 (−0.095,+0.477,+0.356)、819mm / pitch +39.0° yaw 0.0° |
> | 桌边 | **有人在对侧** |
>
> ### 1. 操作者要的那个改动做了，而且它不是卡点
>
> 「取消解支绑定，坐标/桌高/tol=70 都不动」：`solve 0.522 -0.180` 把 `pre["q"]` 只当
> **种子**、不再要求 `max_dev≤0.6`。结果 **31s / 残差 0.00mm**，而且自由解**照样落在
> 同一解支**上（max|q_pre−q_grasp| = 0.239 rad ≪ 0.6）。
> ⇒ 上一节那句「唯一的卡点是解支绑定 / 🔴 无解 407s」是 **IK 搜索抽风**，不是几何不可行。
> 两道安全闸门（`plan_path` 碰撞、`path_floor_check` 指尖地板）一个都没放宽。
>
> 全序列跑完：`pregrasp` (+133.0mm) → `descend` 2 段垂直下降 (+13.0mm，桌外航点 0 个)
> → `close` **828→4mm** → `lift` → `park`。
>
> ### 2. 爪合空了 —— 手眼误差第一次被量到
>
> 手臂退场后无遮挡重测：黄积木 `(0.524,-0.180)`，瞄准点 (0.522,-0.180)、FK 两指中点
> (0.522,-0.179) —— **只差 2.2mm**。瞄得准，爪也真闭到 4mm，积木**一动没动**。
>
> 判据不靠人瞄准：`perceive` 一直把两片**橙色夹垫**当橙色积木候选（然后以 `太靠中线`
> 拒掉）。它们相距 53.1~53.3mm（G2 开合量级；桌上真积木彼此都 >100mm），闭合后两个
> blob 合成一个。obs #10/#11 两次一致：
>
> ```
> 夹垫中点 (0.468,+0.010)  vs  FK 两指中点 (0.522,-0.179)  ⇒ dx=-54  dy=+189mm
> ```
>
> dy 稳健、dx 不稳健（夹垫是射线投桌面定位的，相机地面点 y=-0.002 ⇒ 外推几乎纯在 x 上；
> 设夹垫高 20/40mm ⇒ dx=-71/-88mm，dy 恒 +189/+188）。
> **38 次 0 成功由此解释，比「相机偏」合理得多**（ZED 只差 2.0mm）。
>
> ### 3. 停在这里，没改模型 —— 需要授权
>
> `path_floor_check` 和碰撞检查评的都是**指尖那个点**。随手加 190mm 偏移会让所有安全
> 闸门去保护一个离真手指 190mm 的幽灵点 —— 那是放宽判据。正确做法是修运动学模型，而
> **单个位姿分不开常量工具偏移和手腕旋转错误**，得再采 2~3 个位姿（换 y、换手腕 yaw）。
>
> 接着测：`python3 scripts/pad_offset.py --run loop-003`（`--demo` 自检钉住首测值）。
>
> ### 4. 这一轮修的三处根因（都在共用函数里）
>
> - `xr1.py grip()`：命令话题 volatile/best-effort 无 latching，进程刚起就 publish 是
>   **静默丢弃**（日志 `before=None` 是铁证）。发之前 spin 等发现完成（≤2s）。
>   第一次 `close` 就是这么丢的，重发才 828→4mm。3s 轮询超时**没动**（没有实测说它短）。
> - `home_safely.py plan_home()`：加横向退场（先出到 x=0.15，桌子在那侧不存在）。
>   预抓取位上直达/抬起/支开/停靠**四条全被拒**（`arm_6_link` 扫桌子近边），
>   `via_lateral(dz=+0.30)->park_open` 通，30 段。放在共用函数里，`act park` 也受益。
> - `agent_loop.py path_tip_stats()`：把**判据**（半平面 `tip_clear_mm`）和**外形**
>   （xyz 包络）分开报。旧的裸 z 差把一条全检查通过的路线标成 `-86.3mm`，其实那航点在
>   桌外身侧空地上。解支自由之后必须看包络：换支的「下降」可能是整臂重构。

# 现状 — 2026-08-13 CST

> ## ⚠️ 右臂现在是「停靠」不是零位；黄积木在 x=0.588，**预抓取到得了、抓取高度无解**
>
> ### 动手前必须知道的机器状态
>
> | 项 | 值 | 说明 |
> |---|---|---|
> | 右臂 | `[0, -1.0, 0, -0.4, 0, 0, 0]` | **park_open**，不是零位。指尖 (0.112, −0.888, 0.885)：在桌面半平面外(x<0.2)且高于桌面 |
> | 右爪 | 839 = 全开 | 上一轮它闭在 4mm 空夹着，已开 |
> | 左臂 / 颈 | 零位 / pitch +0.681 yaw 0 | |
> | 服务 / TF / 录像 | 三个 active；45 frames 含 **6 个 zed**；recorder idle+authorized | |
>
> ### 三条这一轮才测出来的硬事实
>
> **1. 右臂零位在碰撞模型里贴躯干 3.0mm，所以没有任何碰撞检查过的路线能回零。**
> `arm_3_link` 余隙 3.0mm < 10mm 下限，出现在 `frac 0.889`（收进零位那一段）。
> 直达 / 垂直抬起 / 肩肘支开 六种中转全被拒。**`xr1.py home` 从不暴露这点**，因为它
> 直接 `move()`、不过碰撞检查 —— 也就是说平时那句 home 一直在擦着躯干走。
> 新脚本 `scripts/home_safely.py` 走 `G.plan_path`（既查碰撞又切段），回不了零就退到
> 上面那个 `park_open`。**没放宽 10mm margin**（同 §55 那条禁令的道理）。
>
> **2. 可达性是高度的函数。** 同一点 (0.5883, −0.1953)
>
> | 高度 | 离桌面 | 结果 |
> |---|---|---|
> | 预抓取 z=0.9438 | +133mm | ✅ 残差 **0.00mm**，dev 1.650rad，从当前位姿规划 OK 11 段（44s） |
> | **抓取位 z=0.8238** | **+13mm** | 🔴 **无解**（tol=45），600s / 8 轮全空 |
>
> 手臂到得了积木上方，下不到顶抓姿态。操作者当面质疑「实际是可以触摸到的」，他是对的
> —— 说的是上面那层。判据直接量：`agent_loop.py act probe <x> <y> --tol 70`。
>
> ### 2026-08-14 用 tol=70 重测，得到一对决定性数据
>
> `tol` 默认已从 45 改成 **70**（操作者授权）：摇操唯一一次真夹住(grip=124)的位姿接近角是
> **59.5°**，45 会把那个已被物理验证的位姿本身判成无解。改的是 `agent_loop.py` 和
> `probe_min_grasp_z.py`；**`grasp_block.py:2121` 的 CLI 默认仍是 45，主流水线没改**。
> 重测结果（都是自由 IK、不限 `max_dev`、指尖中点、y=−0.187）：
>
> | 目标 x | 预抓取 +133mm | **抓取位 +13mm** |
> |---|---|---|
> | **0.589**（积木现在的位置） | ✅ 0.00mm / 12s | 🔴 **无解**，331s / 8 轮全空 |
> | **0.480**（往近侧推 11cm） | ✅ 0.00mm / 2s | ✅ **0.00mm / 24s** |
>
> **积木已在 13:21~13:52 之间被挪近，现在在 x=0.522 −0.179**（同一条感知路径
> `zed_pointcloud_bbox`、同一头位姿，像素从 (693,265) 变到 (703,307)、面积 738→867
> ⇒ 是物体真的动了，不是两套算法不一致）。所以：
>
> | 目标 | 抓取位 +13mm，tol=70 自由 IK | 抓取位，tol=180 | 预抓取→抓取同支 (`max_dev` 0.6) |
> |---|---|---|---|
> | x=0.589（13:21 的位置） | 🔴 无解 331s | — | — |
> | **x=0.522（现在）** | ✅ **0.00mm / 22s** | ✅ 0.00mm / 19s | 🔴 **无解 407s** |
> | x=0.480 | ✅ 0.00mm / 24s | — | — |
>
> **⇒ 抓取点本身已经可达；现在唯一的卡点是「先取预抓取解、再要求抓取解每关节差 ≤0.6rad」
> 这条下降链。** ⚠️ **后半句已作废**（08-14）：取消绑定后自由解 31s/0.00mm 且**照样同支**
> (dev 0.239)，那个「无解 407s」是 IK 抽风。真卡点见最上面那节的 190mm 指尖模型误差。 也就是 `--clear 0.12` 那个**世界 z 轴垂直下降 12cm** 的固定形状太僵，
> 摇操走的是斜向接近（那次真夹住的接近角 59.5°）。08-13 那句「x=0.48 抓取高度也无解、
> 推积木救不了」是 tol=45 量的，**已作废**。
>
> 顺带把 `BAND_X` 的地位定下来：**它的上界 0.56 在抓取高度这一层被实测支持**
> （0.589 ✗ / 0.480 ✓），错的是拿它当所有高度的判据 —— 预抓取那层 0.589 有解。
> 「能碰到」和「能抓」要分两层问，判据 `agent_loop.py act probe <x> <y> --tol 70`
> （有解 2~24s，无解要烧 ~330~600s，耗时本身是信号）。
>
> nudge（机器人自己推积木）那段已删：它要把爪子伸到积木后面 x≈0.651，run 26 判无解、
> run 29 反而把积木从 0.524 推**远**到 0.588 —— 让人推 2 秒的事，不值得再测 tol=70。
>
> ### 今天 30 次运行全部困在同一条代码路径
>
> `BAND_X` 预筛丢掉积木 → 候选空 → 触发 nudge → nudge 无解 → `out_of_reach` exit 7
> （`grasp_block.py` 2310–2344）。另外 run 29 感知到 **0 个 yellow 候选**的真因是
> **手臂停在 ZED 视野里**，橙色夹垫被当成候选（`20260813-29/view_after_nudge.jpg`
> 一眼可见）—— 又一次印证「先看原图」。run 30 死在 `record_start_failed:
> stop ignored: not recording`，但 `xr1_cam.py doctor` 全绿（auth=authorized、
> state=idle、skew −1s、FHD C3 1920x1080@30），所以那是调用方的瞬时问题，
> **`require_video=True` 不会直接判废**。
>
> ### 没做什么
>
> `fast_yellow_grasp.py` **没写**：指令里的代码块是空的，而它四条策略里三条被实测否掉
> —— Nudge Sweep（run 26/29 已证死）、D455 下探补偿（对齐深度 <250mm **零像素**，
> 最近 295mm，合拢那 25cm 必然开环，该用 D405）、合并航点消除中间停顿（会删掉唯一
> 能防压桌的碰撞采样点，`effort` 全是 `.nan` 摸不到接触力）。剩下「极速 IK」值得做，
> 但本机单次 IK 是分钟级不是 100ms，那是算法工作不是参数工作。

# 现状 — 2026-08-12 19:2x CST

> ## 🔴 四条判据全部**未取到**：机器人一次都没动，在动之前就被自己的地板检查拒了
>
> 今天的目标是「让右爪真的闭合在那块黄积木上」。结论：**没闭合，也没动**。
> 三次运行(两次我的、一次别的会话的 dry-run)全部止步于**执行前**的静态地板检查。
> 下面四条是原定判据，逐条对账，一条都不许用「没报错」替代：
>
> | # | 判据 | 实测 | 为什么 |
> |---|---|---|---|
> | 1 | `wrist_track` 至少一个路点最大连通域 ≥800 px | **未取到** | 沿路采样发生在动起来之后，没走到 |
> | 2 | 抓取高度腕部闭环 `off_aim` 收缩 | **未取到** | 同上 |
> | 3 | 闭合后夹爪读数 20 < mm < 800 | **未取到**；全程 **838**(全开、空) | 没发过闭合指令 |
> | 4 | 抬起后 ZED 里黄色跟着手走 | **未取到** | 没抬 |
>
> 机器人**完全没动过**：右臂零位、双爪张开 838、头 pitch +39.0°/yaw 0.00°、手里没东西。
> 拒动是**正确**的，见下。
>
> ### 根因：「抓取解贴着预抓取解找」只是注释，不是约束（全文 PITFALLS §55）
>
> `20260812-16` 的 `plan` 事件自称满分(`ik_err 0.0mm`、`tip_resid 0.00036mm`、
> 指尖高出桌面 13.0mm、余隙 10.55mm)，却被地板检查以 **7.4mm < 8mm** 否掉 ——
> 只差 0.6mm，看着像闸门太严。把记下来的 `q_pre`/`q_grasp` 拿出来一比就清楚了：
>
> ```
> 逐关节 |Δ| [2.989 2.617 1.748 0.084 1.212 1.317 0.688]  max 2.99 rad
> 两端指尖 (0.5397,-0.1584,0.9438) -> (0.5397,-0.1584,0.8238)   # 竖直 12cm
> 中间 20 段 指尖 y∈[-0.813,-0.158] z∈[0.810,1.241]            # 甩出去 65cm 再擦回来
> ```
>
> 那 12cm 的「下降」实际是**整臂重构**：抓取解落在和预抓取**不同的解支**上。
> 三道判据各看一部分，谁都没拦住：`ik_center` 按**残差**挑(两支都是 0.0mm)、
> `collide()` 查**连杆**(指尖中点不是连杆，`tcp_link` 是单根手指)、只有
> `path_floor_check` 看指尖中点 —— 而它在选完路线**之后**才跑，只有一票否决权。
>
> ### 改了什么（三处，**全部是收紧**，没有放宽任何现有判据）
>
> 1. `ik_center(..., max_dev=)`：超出的解不许当候选(不是事后否)，精修也不许推出去。
>    `DESCEND_MAX_DEV = 0.6` rad(12cm 落在 ~0.5m 臂上约 0.24 rad/关节，留 2.5 倍)。
> 2. 调用处再核 `dg` —— 它本来就是 `ik_center` 的返回值，以前没人看。
> 3. `plan_to(..., accept=)`：路线拼好后过一次指尖地板检查，不通过就**试下一条**。
>    自检 `scripts/test_plan_to_accept.py`(4 条断言，假 `plan_path` 绕开 URDF/IK，秒级)。
> 4. 中转点多两个高度 + 改成**按需解 IK**(每次分钟级，先试的路线用不到就别解)。
>
> ⚠️ **没人授权就不许**放宽 `path_floor_check` / `tip_clear_mm` / `MIN_TIP_CLEAR`。
> 上面每一条都是让判据参与搜索，不是让判据让步。
>
> ### 唯一还没定的事(验证在跑)
>
> **x=0.54 这个目标在同一解支下到底有没有抓取解？** `/tmp/descend_fix.py` 复用
> `20260812-16` 的 `q_pre`(省掉 35 分钟的预抓取 IK)，只重解抓取那一步，三条实测判据：
> 单关节位移 ≤0.6 rad、指尖残差 ≤15mm、下降段地板检查无违规。
> **无解的话下一步是挪底盘/把积木推近，不是改判据** —— 可达带是 x∈(0.18,0.56)，
> 积木在 0.540，离边缘 20mm。
>
> ### 试验账本(今天下午到晚上)
>
> | 目录 | 谁 | 结果 | 关键数 |
> |---|---|---|---|
> | `20260812-16` | 别的会话 dry-run | `refused` | `orient`→`plan` **2118s**；via `lateral_high`；地板 7.4mm |
> | `20260812-17` | 我，第一次 | 我 kill 的 | 旧路线代码，必拒 |
> | `20260812-18` | 我，第二次 | 我 kill 的 | `high_over_target` 在 x=0.54 **无 IK 解** |
>
> **感知这一环是干净的**：三次独立运行给出同一块积木 x=+0.540 y=−0.158
> (rise +3.0cm、深度有效 100%、area 864/865、长轴 154~157°⇒开合 64~67°)，
> 圈图(`/tmp/view_marked.jpg`)确认是白桌上真实的小黄积木，不是果篮也不是夹爪橙垫。
> `USE_HANDEYE = False` 保持不变。
>
> ### 一个静默降级，记着
>
> 这一轮出现 `[warn] using built-in joint limits (robot_state_publisher not answering)`：
> rsp 活着、`/robot_description` 话题也有内容，是负载(load 25)下 `get_parameters`
> 没在 3s 窗口内应答。落回的 `FALLBACK_LIMITS` 和补过的 URDF 数值一致(头 ±0.698、
> 右臂 2/4 非对称)，所以这次无害 —— 但它是**负载相关**的，重负载下会静默发生。
>
> ### 顺手记的另一条
>
> 零位→中转那条腿通不通**不是高度的单调函数**：gz+0.30 撞(`arm_5_link` 插进桌面
> 273.8mm)，而更低的 gz+0.15 和更高的 gz+0.45 都通 —— 是解支问题(PITFALLS §54)。
> 注意 §54 里"三条路线全废"是**那个旧目标**(0.360,−0.183)的事；在 (0.540,−0.158)
> 上 `lateral_high` 是通的，真正的卡点是下降段(§55)。

# 现状 — 2026-08-12 17:2x CST（**已被下面…上面 19:2x 一节覆盖，先读那一节**）

> ## 🟢 根因找到了：手眼「修正」自己就是那 230mm —— `USE_HANDEYE` 现在默认关
>
> 38 次 0 成功 + 今天两次自我拒绝，链条上每一环都自称正确（检测像素落在积木上、
> IK 残差 0.0mm、地板检查过、FK 反算残差 0.0mm），却在同一时刻的 ZED 照片里
> **夹爪根本不在取景框内**。判据是 `scripts/zed_hand_probe.py`：把夹爪停在 5 个
> FK 已知位姿当标记物，比"模型预测像素"vs"夹垫实测像素"，**不做任何拟合**。
>
> | | Δu 均值 | 结论 |
> |---|---|---|
> | 套 `handeye/correction.json` | **+228.5 px**（5 位姿同号） | 修正**本身**是误差源 |
> | 不套（原始相机模型） | **−9.6 px**（std 11.3） | ✅ 这才是对的 |
>
> 折到目标：黄积木修正后 (0.396,−0.435) vs **不修正 (0.360,−0.183)** —— 差 230mm。
> `correction.json` 自称 `no_correction_loo_mm=218.85`，**和实测反号**；它的 LOO 11.24mm
> 是 15 个"积木举在空中"样本的盒内自洽度，不是对世界的正确性。
> 文件已盖 `VOID` 章但没删，全文见 **PITFALLS §53**。
> ⚠️ 别再在任何输出/判据里引用"LOO 11.24mm"当误差量级。
>
> ### 关掉修正之后的新卡点 → 已修
>
> 目标落到 **(0.360, −0.183)**（可达带内、面积 1478、长轴 135°⇒开合 45°），但规划失败：
> 从零位出发的关节直线在 50% 处 `arm_3_link` 插进桌面 62mm，而**起点就是 `STAGING_Q`**，
> 唯一的中转点退化 ⇒「没有第二种走法可试」。
> 修法（用户已授权"也可以尝试不同轨迹"）：`plan_to(..., vias=)` 支持额外中转位姿，
> 调用方给一个**和目标相关**的 —— 指尖在积木正上方 `HIGH_VIA_M=0.30`m 的位形，
> 种子取 `pre["q"]` 保证同一解支。它不随起点退化。自检 `scripts/test_plan_to.py`
> 六条断言（含 08-12 这个死局的重现）。
>
> ### 腕部相机改成**全程同步定位**（用户要求）
>
> 接近路径每到一个路点就拍一张腕部图，量目标色**最大连通域**（不是整幅掩膜 px，
> §52 那 71 px 的教训）+ 离准心偏移，落 `wrist_track`。下降闸随之改成两级：
> 预抓取位看不见、但沿路某个路点看清过（≥800 px）⇒ 判为**视场问题**（§52：腕相机看的是
> 爪子前方）放行到抓取高度，由腕部闭环判（它自己会拒，`20260812-11` exit 6 就是它拒的）；
> 两个高度都没看清过 ⇒ 才是"积木不在"，拒。
>
> ### 机器现在的状态
>
> 右臂零位（`xr1.py home` 过）、双爪张开、头 pitch +39.0°/yaw 0.00°、手里没东西。
> `USE_HANDEYE = False`（在 `scripts/grasp_block.py`，改它之前先读它上面那段）。

# 现状 — 2026-08-11 18:15 CST（**已被上面 08-12 17:2x 一节覆盖，先读那一节**）

> ## 🔴 2026-08-12 16:05 收摊：摇操栈已全部拆掉，ZED 已还回；**手臂今天一次都没动过**（这一节最新）
>
> ### 机器现在的状态（可以直接接手）
>
> | 项 | 状态 | 复验过的证据 |
> |---|---|---|
> | dora 采集图 | 🔴 已拆 | 按 PID 段收（823538 SIGTERM，823540/823795/823800/823802/823804/823807/823808 `kill -9`），只剩 `livekit_server` |
> | `Astrabot_LiveKit` / `_LiveKit_Srv` | 🔴 inactive | 拉起前就是 inactive，已还原 |
> | `Astrabot_ZED` / `_ZED_Points` | ✅ active | **6 个 zed frame**（正常 6）、static frame 共 23、`/zed` 话题 38 个 |
> | 右腕 D455 | ✅ | 彩色 **8.852 Hz**、对齐深度 12.487 Hz |
> | 右臂 | 零位 | 摇操从没驱动过它，**不需要 `xr1.py home`** |
>
> ⚠️ 数 zed frame **不能**用 `ros2 topic echo /tf_static --once`（只收到一个发布者的一批，
> 得到 2，长得像 §38 那个「光学系 frame 整棵消失」的故障）。必须用 TRANSIENT_LOCAL QoS
> 订阅 `/tf_static` 转 5~6 秒再数。
>
> ### 摇操没跑通，故障在 Quest App 侧（机器人侧全绿）
>
> 机器人侧正面判据全过：LiveKit media+token 活、`POST .102:5000/token` → 200、
> 9/9 dora 节点 ready、`request_control` RPC 已注册、零错误、两条已知静默故障都堵住了
> （`use_byte` 通道名两侧对上、两个 `teleop_gripper_float` 订阅者数各 1）。
> 但 `teleop` 节点收到的是 **`len=9 first_byte=0x68 head_repr=b'hello wor'`** ——
> 字面意义的 `hello world` 心跳，间隔 4~20 秒。`TeleopFrame` 只有 `#1 header`/`#2 data_body`
> 两个字段，合法首字节只能是 `0x0a`/`0x12`，`0x68` 是 field #13 ⇒ **它不可能是任何版本的
> TeleopFrame**。App 连上了 LiveKit 但没进摇操流。三层判据表已写进
> `docs/06_teleop_bringup_sop.md` **§5.1.1**。
>
> ### ⚠️ 订正：下面 §1 那句「摇操 🟢 正在运行（本日 10:39 起）」是**未验证的断言**
>
> 它的依据只有「4 路 track 全推、通道名对上」，**没有任何一条记录证明手臂真的动过**，
> 而那次的 dora 日志已经不在磁盘上了。所以「摇操跑通过」现在只能算历史断言，
> **别拿它当基线**。真正的基线是：手臂动一下，指尖 FK 从 `(-0.050, -0.466, +0.353)` 变掉。
> （同类错误见 memory `xr1-external-camera-recorder`。）
>
> ### 真值采集：两次共 70 分钟，**0 条抓取**
>
> `scripts/teleop_grasp_record.py`（只读）跑了 25 min + 45 min，因为摇操从没驱动手臂，
> `grasps.jsonl` 是空的。求解器 **`scripts/wrist_solve.py` 已写好并 import 通过**，
> 一旦录到一次真实抓取就能直接出 `T_tcp_cam` + 桌面平面（自检是
> `Δz ≈ +27mm`，即「顶面质心 − 两指中点」）。所以现在的**唯一卡点是人能摇动手臂**，
> 不是代码。

> ## 🔴 2026-08-12 14:10 卡点定案：`path_floor_check` **误拒** —— 它把机器人**右胯外侧的空地**当成桌子(这一节最新)
>
> `20260812-05` 那次 `--dry-run` 的拒绝理由是「路径第 0 个航点指尖低到桌面上方 0.5mm」。
> 把那个航点的位置算出来（`/tmp/way0.py`，只读，不动机器人）：
>
> | 航点 | 指尖 (x, y, z) | 高出桌面 | 那里有桌子吗 |
> |---|---|---|---|
> | 起点(零位) | (−0.050, −0.466, +0.353) | −458.1 mm | 没有（x<0.2） |
> | **way0 ← 被拒的就是它** | **(+0.047, −0.827, +0.811)** | **+0.5 mm** | **没有（x=0.047 < TABLE_X_MIN=0.2）** |
> | way1(预抓取) | (+0.377, −0.401, +0.944) | +133.0 mm | 有 |
>
> 手臂从零位摆到预抓取位，中途从**右侧甩出去** y=−0.827、x≈0，那是机器人自己胯
> 外侧的空地，桌子在 x≥0.2。**桌上那段整条线最低点是 +133 mm**，离桌面远得很。
>
> 根因是两个判据不一致：`collide()`（真正建了桌子模型的那个）知道
> 「桌面只在 x ≥ `TABLE_X_MIN`=0.2 存在」，而 `path_floor_check()` → `tip_clear_mm()`
> **只比 z、完全不看 xy**。所以整条路径 `collide()` 全过（`plan_to` 返回了路径），
> 却被后面那道更粗的地板检查拦下。**一处三行的判据 bug，不是感知、不是标定、
> 不是可达性。** 建议改法：`tip_clear_mm` 在 `x < TABLE_X_MIN` 时返回 `+inf`
> （和 `collide()` 用同一个半平面模型，别再各写一套）。⚠️ 这是放松一道安全检查，
> **没人同意之前不改**。
>
> 顺带的两个数（同一次 dry-run，都还成立）：抓取位余隙 13.0 mm、指尖残差 0.0 mm、
> 手眼 LOO 11.24 mm、4 个在可达窗口内的候选（x=0.377/0.447/0.498/0.550）——
> 12:45 那节说的「桌子不重叠」在这一帧**已经不成立了**，桌子够近了。
>
> **右腕 D455 现在对几何贡献为零**：22 个话题都活着、RGBD 都在发，但 TF 里
> 45 帧一个 `d455_*` 都没有（`publish_tf:=false`，而且腕部外参根本没标）。
> 详见下面 13:20 一节。

> ## 🔴 2026-08-12 12:45 卡点定案：**桌子不在可达范围里**，不是标定问题(已被 14:10 覆盖：那一帧桌子已在窗口内)
>
> ### 需要人做一件事（机器人自己做不到）
>
> 1. **把白桌往机器人方向推** —— 桌子带脚轮，被推远了。目标：桌上积木落在
>    `x ∈ 0.30~0.50`（现在实测 **0.745~0.881**，抓取窗口是 `x ∈ (0.18, 0.56)`，
>    **完全不重叠**）。或者改为挪底盘，别转头（转头 40° = 半米误差）。
> 2. **把掉在地上的黄积木捡回桌上** —— 见下面「我把积木掉了」。
>
> 做完这两件，`grasp_block.py` 可以直接跑，链路本身是通的。
>
> ### 三个实测结论
>
> **① 桌上积木在 x=0.745~0.881，抓取窗口是 x∈(0.18,0.56)。** 不重叠。
> 这比手眼标定根本得多：标定再准也抓不到不在可达范围里的东西。38 次 0 成功
> 大概率一直是这个原因，而它**伪装成标定误差**。
>
> **② 深度这次是对的，可以当独立裁判。** 同两块积木两种独立算法：
>
> | 物体 | 射线求交 x | 纯深度反投影 x | 深度给的 z |
> |---|---|---|---|
> | 桌上红积木 | +0.780 | **+0.745** | **+0.836** |
> | 纸上绿方块 | +0.881 | **+0.837** | **+0.838** |
>
> 差 35~44mm；顶面 z=0.836/0.838 = 桌面 0.811 + 25mm 积木，和摇操真值锚点
> 0.8108 完全吻合。**所以"深度有 +62.8mm 偏差"在这个头位姿下不成立**，
> 以后别拿它当理由跳过深度交叉校验 —— 一开始做这一步就不会掉积木。
>
> **③ 之前那个"完美方案"是去抓它自己手里那块。** 桌子推远之后，ZED 在
> x=0.36~0.55 看到的候选**全是机器人自己的手**（两片橙色夹垫 + 手里的黄积木）。
> 12:35 那一节记的"残差 0.0mm、余隙 13mm、全检查通过"的方案，目标是自己的手。
> 自指过滤只滤掉了 3 个里的 2 个。**判据：手臂在视野里就先 `home` 再感知**，
> 物理排除比写代码过滤可靠。
>
> ### 我把积木掉了（要人捡）
>
> 按用户批准的「一次做完：放下并抓」执行了 `place_calib_block.py --go --release`。
> 执行本身**完美**：松手时 FK = (+0.4614, -0.24839, +0.8238)，离目标 **0.01mm**，
> 真值已落盘 `experiments/handeye/placed_truth.jsonl`。
> **但那个位置没有桌子** —— 积木凌空掉了约 0.8m 到地毯上。
> 现在它是胸部鱼眼画面里机器人正前方约 0.45m 处那块**黄积木**
> （ZED 看不到：x=0.46 的地面在画面下边缘之外）。
>
> ### 本次还修了 `/joint_states` 假死
>
> `ros2_control_node` **从自己的 launch cgroup 里消失了**，而 systemd 仍报 `active`
> （launch 不会因为一个节点死掉就自杀）。判据是数 cgroup 成员，不是 `is-active`：
> `systemctl status Astrabot_Controller --no-pager | grep -E "^\s+[├└│]"`，
> 健康时 5~6 个成员。重启控制器后 ~80s 到 185Hz。**不用管 g2_gripper_node** ——
> 颈部在 ttyAMA10，和夹爪不冲突（三次实测），让它跑着反而保住手里的东西不掉。
> 重启后颈部归零，必须补 `xr1.py look 40 0` 再 `xr1.py home`。
>
> ### 恢复现场
>
> 双臂零位；头 pitch 39°/yaw 0；右爪 **829mm（空、张开）**；左爪 824mm。
> 手眼修正 `correction.json` 仍然有效（TF 与标定时**逐位相同**，origin 差 0.0mm）。

> ## 🟡 2026-08-12 12:35 全链路只规划不执行跑通（**目标是自己的手，见 12:45 一节**）
>
> 修正注入之后第一次把整条流水线跑到底（`--dry-run`，没发任何运动命令），
> **它成功了** —— 这是 38 次 0 成功以来第一次拿到一个通过全部检查的完整方案：
>
> ```
> [手眼] 已修正 M4 全刚体，光心挪 [54.9, -133.6, -20.8] mm；标定精度 LOO 11.24 mm(最差 22.95)
> 目标 orange @ x=+0.445 y=-0.056 -> right 臂 (指尖残差 0.0mm, 接近角 37°, 余隙 28mm)
>   tcp 去 (+0.454,-0.099,0.855) -> 指尖中点 (+0.445,-0.056,0.824); 桌面 0.811, 指尖高出 13mm
>   地板检查: 抓取位余隙 13.0mm, 全路径最低点 13.0mm (第 24 航点) -> 通过
> ```
>
> 指尖中点落在 **0.824** —— 和真值锚点 `GRASP_TIP_Z=0.8238`（人摇操摆出来的「能夹住」
> 位姿）差 0.2 mm。z 链是自洽的。
>
> ### 看图比读日志有用：`/tmp/grasp_view.jpg` 一眼定了两件事
>
> 白桌上**只有两块真积木**：红橙长条 (0.445,−0.056) 和绿方块 (0.517,−0.278)。
> 其余落点分别是**水果篮里的塑料果蔬**（x 0.74~0.95，已被 `x 超范围` 挡掉）、
> 远处绿桌上的碗，以及 —— 关键 —— **右爪自己**：
>
> | 感知落点 | 其实是什么 | 离右手指尖中点 |
> |---|---|---|
> | yellow (0.390,−0.379) | 手里夹着的标定积木 | ~0 mm |
> | orange (0.376,−0.406) | **夹爪自己的橙色夹垫** | 30 mm |
> | orange (0.362,−0.369) | **夹爪自己的橙色夹垫** | 30 mm |
>
> 三个"完全合格"的候选全在自己手上。夹垫是橙色的这件事让"排除自己"从一条
> 保险变成了**必需品**。
>
> ### 因此改了两处（都已落代码，`grasp_block.py`）
>
> 1. **排除自己的手。** 夹爪读数落在夹持带 ⇒ 手里有东西 ⇒ 排除离该手指尖中点
>    <60 mm 的候选。判据**必须双侧夹逼**（读数是**开口**：空爪合拢 6~7 / 夹住
>    40mm 积木 125 / 全开 838~840，所以 `pos>20` 会把全开判成夹住，正好判反）。
>    **两只手都查** —— 哪只手臂去抓是按候选的 y 符号定的，所以在排除的时候
>    `side` 还不存在（第一版就这么崩的：`UnboundLocalError`），而夹垫被误检和
>    左右哪只手要动毫无关系。
> 2. **候选按「离手眼标定盒多远」重排**（同档内才按面积）。原来是纯面积降序，
>    于是流水线挑中了 **橙(0.445,−0.056) —— 它在标定盒的 y 方向外面 192 mm**，
>    而绿(0.517,−0.278) 只外推 56 mm。LOO 的 11.2/23 mm 是**盒内**成绩，盒外
>    没有任何证据支持它，而 M4 含 −15.45° 偏航，离标定区越远模型自身的
>    不确定度越大。**不是硬否决**（盒外也可能抓得到，否决了就没候选了），
>    只是让第一次自主抓取落在最有据可依的那块上。
>    `apply_handeye()` 的 info 现在把 `fk_bbox`/`range_m` 一起交出去，
>    下游能自己判内插还是外推，不靠人记得。
>
> ### 唯一的卡点：**必须松手，而松手要人同意**
>
> `grasp_block.py` 不带 `--dry-run` 的第一个动作就是 `r.grip(side, 0.0)`（张开），
> 右爪还夹着标定积木。桌上那两块真积木都在右臂一侧（y<0），左臂到不了，
> 所以**绕不开**：要自主抓，就得先把手里那块交出去。
>
> 交出去有两种方式，代价差一个数量级，已经写好了好的那种 ——
> **`scripts/place_calib_block.py`**（默认 dry-run，要动得显式 `--go --release`）：
>
> | | 原地张开 | `place_calib_block.py` |
> |---|---|---|
> | 积木去哪 | 从指尖高度掉下去、弹、滚走 | 先把两指中点降到 `GRASP_TIP_Z`（底面贴桌）**再**松手，不下落不滚 |
> | 换来什么 | 「桌上某处有块积木」，信息量等于标定前 | 松手瞬间的 FK = **积木真实位置**，一个**留出样本**(held-out) |
> | 放在哪 | 随机 | 标定盒内**离所有 15 条标定样本最远**的点(maximin)，候选按 maximin 降序逐个试 IK+规划，取第一个走得通的 —— "最远"让位于"到得了" |
>
> 这正好补上卡点 #3 缺的那把尺子：标定用的 15 条全参与了拟合，而放下这一块
> 之后感知去看它，链里依旧没有人的瞄准、没有第二套外参，**是真正的留出点**。
> 判据用 LOO **最差值 23 mm** 而不是 RMS —— 抓取是最差情况决定成败的。
> 而且它同时把标定物变成了第一个抓取目标：抓成了，积木就回到手里。
>
> 🔴 **松手是不可逆的**（要靠人摇操才能把积木塞回手里），而且 08-11 的
> 「别开右爪」是另一个会话立的规矩。所以**没有人明确同意之前不执行**。

> ## 🟢 2026-08-12 11:50 手眼标定**做完了**，卡点 #1 关闭(这一节最新)
>
> §4 里排第一的「手眼误差一次没量到」**已经量到了**，而且已经注入感知链。
> 38 次 0 成功的主因就是它：TF 给的相机位姿错了 **219 mm**。
>
> | | |
> |---|---|
> | 标定物 | **右爪里那块黄积木**。夹住 ⇒ 指尖中点 = 积木真实位置，链里既没有人的瞄准也没有第二套外参，所以**自证** |
> | 样本 | 15 条（重启前 5 + 08-12 宽基线 10），基线 123×235×166 mm，距离 0.477~0.743 m(变化 42%) |
> | 定案模型 | **刚体**（相机位姿整体错了）。RPY (−1.99, −4.92, **−15.45**)°，光心被搬 146 mm |
> | 精度 | **留一交叉验证 11.2 mm，最差 23.0 mm**（不修正是 218.9 mm） |
> | 产物 | `experiments/handeye/correction.json`（含标定时头部姿态、样本包络、range） |
> | 注入点 | `grasp_block.perceive()`，`apply_yaw_fix` 之后 `apply_handeye()` 左乘到相机位姿上 |
>
> **为什么左乘 TF 而不是给落点加偏移**：样本里的 `P_zed` 是反投影出来的三维点，
> 所以「点的刚体修正」＝「相机位姿修正」。左乘一次同时修好 origin **和射线方向**，
> 射线重解 `s` 后落点仍**精确落在桌面平面上**（实测 z 位移 = 0.0 mm，可作为注入
> 位置对不对的判据）。给落点加偏移做不到：方向没修，z 还会被推离平面。
>
> **它只在标定时的头部姿态下成立**（pitch **39.0°** / yaw **0.0°**）。相机外参是头部
> 姿态的函数。`apply_handeye()` 会自己校验，差 >1.5° 就**拒绝修正并喊出来**，
> 不会悄悄套一个不成立的变换。换了头部姿态就得重标。
>
> | 复算命令（都不动机器人） | |
> |---|---|
> | 重新拟合/换模型 | `python3 handeye_fit.py --cam 0.12171 0.05861 1.27247 --head 39.0 0.0` |
> | 看修正前后差别 | `python3 handeye_check.py`（同一帧只换相机位姿，只读） |
> | 只规划不执行 | `python3 grasp_block.py --dry-run --no-video` |
>
> ⚠️ **两个还没解决的坑，动手抓之前必读** —— 第 2 条 12:35 已修，见最上面那一节
> 1. **不带 `--dry-run` 跑 `grasp_block.py` 的第一个动作就是 `r.grip(side, 0.0)`
>    ——「张开」**。右爪还夹着标定基准，所以那一下就把它扔了。
>    （12:35：仍然如此。已写 `place_calib_block.py` 用"放"代替"扔"，但**要人同意**才跑。）
> 2. ~~**感知链没有「排除自己手里那块」的判据。**~~ **12:35 已修**（而且实测要排除的
>    不止积木本身，还有**夹爪自己的两块橙色夹垫**，共 3 个假候选）。
>    手里那块黄积木在 `why_reject()` 眼里是完全合格的候选（x 在带内、不贴边），
>    实测它被感知到 (0.3903, −0.3789)，而 FK 给 (0.3916, −0.3722) —— 差 6.8 mm，
>    顺便成了修正接线正确的旁证（in-sample，只证接线不证精度）。
>
> **外推要小心。** 标定包络只有 x [0.338,0.461] / y [−0.483,−0.248]；离开它 M4 的
> −15.5° 偏航会放大，实测同一帧不同位置的修正位移从 213 mm 到 617 mm 都有。
> 桌上作业区正好在包络内(是**内插**)，但画面远处那些落点(x 到 1.5 m)别当真。

> ## ⚪ 18:15 当前跑着什么(2026-08-11，**整节已过时,连夹爪那条也已作废**;现状看顶部 08-12 16:05 一节)
>
> **dora 采集图已全部停掉,ZED 服务已还回。** 摇操/数采按操作者 18:00 的话
> (「摇操停了不需要,也不需要上传数据和数据采集上传」)停着,`/zed/...` 话题回到图上。
>
> | 现在的状态 | |
> |---|---|
> | ~~🔴 **右爪正夹着一块黄色积木 —— 别开它**~~ **已作废** | 那块积木 08-12 被 `place_calib_block.py --go --release` 松手掉到地上了(松手位置本身完美,离目标 0.01mm,但桌子被推远)。**08-12 16:0x 实测 `/qg_robot/gripper_right_state` = `[839,0,0,0]` = 全开,右爪空的**。原文那句「840=全开所以它确实夹着」现在只剩一个可复用判据:`pos` 明显小于 839 才算夹着 |
> | ✅ ZED 服务 active,zed frame = **6** | 18:13 复验:TF 共 43 帧,6 个 zed frame 全在。光学系名字是 `zed_left_camera_frame_optical`,**不是** `..._optical_frame` |
> | ✅ dora 图已全停且干净 | `dora run`(4150372)+ 10 个子节点。SIGTERM 父进程后子节点**全部孤儿化到 ppid=1 继续跑**,按名字 `pgrep` 一定漏 —— 按**启动时的 PID 段**(4150300–4150800)枚举再 `kill -9` 才干净(§51) |
> | 🔴 ZED 服务是 `Restart=always` / `RestartUSec=100ms` | 松手 100 ms 内 systemd 就抢回相机。想独占(pyzed)**必须显式 `systemctl stop`**,kill 进程没用;显式 stop 不触发 `Restart` |
> | ⏸️ `Astrabot_ZED_Points` 仍是 `failed` | 它**在我进场前就 failed**,不是停 ZED 造成的。我 `reset-failed` 了但**没有 start** —— 要点云得自己起 |
> | ✅ 别人的东西逐个复查完好 | `nav2_container`(7160)、`ros2_control_node`(2639547)、`robot_state_publisher`(2639548)、G2 夹爪驱动(2649194) |
> | ⏸️ `/dev/shm` 没清 | 授权里有这一项,但**我没做**:129M / 62G(1%),零空间压力,而当时 21 个活着的 DDS 进程,盲删有实际风险。那些 `RTPS_TRANSPORT_SHM ... open_and_lock_file failed` 是噪音,真凶是 `ROS_DOMAIN_ID` |
>
> 要再起 dora 的话:三个前置条件缺一不可,而且**每次报错都指向错误的方向** ——
> 配方在 **`PITFALLS.md §51`** 和记忆 `dora-graph-restart-three-preconditions`,别自己试
> (我试了四次)。启动前必须先 `systemctl stop` ZED,那会让整张图上的 `/zed/...` 消失,
> 是**对外动作**,先看有没有人在用。
>
> ### 🟢 2026-08-12 13:20 右腕改用 Intel RealSense D455,驱动已装好、RGBD 已出话题
>
> 操作者定案:「**更新配置以后右手新的摄像头就用这个 Intel RealSense RGBD**」。
> 下面 08-11 那条「右腕永久只有 DW2 / 没有彩色」**已被这条取代**,但坑还在,照抄:
>
> | 事实 | 值 |
> |---|---|
> | 设备 | `8086:0b5c` D455,SN `311322304307`,固件 `5.13.0.50`,USB 口 **1-4.2.3** |
> | 装在哪 | 画面里有**同侧橙色夹垫 + 圆形夹爪底板 + 地毯** ⇒ 已经是眼在手上;**哪只手还没器械验证**(两侧夹爪当时都全开,靠"有没有黄积木"分不出来) |
> | 驱动 | 08-12 13:14 `apt install ros-jazzy-realsense2-camera`(4.58.1 + librealsense2 2.58.1)。**源里现成有,不用编译**;顺带升了 7 个 mesa 补丁(操作者批准) |
> | 话题 | `/right_wrist/d455/color/image_raw`、`/right_wrist/d455/aligned_depth_to_color/image_raw`(16UC1 mm)、`.../depth/image_rect_raw`、`extrinsics/depth_to_color` |
> | 实测 | 640×480,彩色 **14.2 Hz** / 对齐深度 **14.1 Hz**;彩色内参 `fx=387.48 fy=386.51 cx=320.87 cy=247.58` |
> | 预览 | `/tmp/robot_cam_preview/d455_pair.jpg`(左彩色右深度,**黑=无深度**) |
>
> ```bash
> ros2 launch realsense2_camera rs_launch.py \
>   camera_namespace:=right_wrist camera_name:=d455 \
>   depth_module.depth_profile:=640x480x15 rgb_camera.color_profile:=640x480x15 \
>   enable_infra1:=false enable_infra2:=false align_depth.enable:=true \
>   enable_sync:=true pointcloud.enable:=false publish_tf:=false
> ```
>
> **🔴 两个坑,选它当抓取深度源之前必须知道:**
>
> 1. **链路只有 USB 2.0**(`speed=480`,驱动自己 `WARN: connected using a 2.1 port.
>    Reduced performance is expected`)。USB2 模式下 profile 被裁:深度最高
>    `848x480@10` / `1280x720@5`,彩色 `1280x720@15`。要全速得换 USB3 口和线。
> 2. **近距盲区正好落在夹爪合拢的位置。** 实测对齐深度:有效 55.5%、**最近 295 mm**、
>    `<250 mm` **一个像素都没有**;预览图里整个夹爪区是黑的。
>    D455 的 min-Z 就是 0.4–0.5 m 量级,**近距变体是 D405(min-Z ~7 cm)**。
>    也就是说 D455 能看清"积木在哪",但手伸到积木跟前的最后一段它会瞎 ——
>    这恰好是 DW2 一直没测出来的那个量,现在在 D455 上测到了。
> 3. IMU 不可用(`No HID info provided, IMU is disabled`):deb 没装 udev 规则。
>    彩色/深度不受影响,要 IMU 得补 `99-realsense-libusb.rules`。
>
> ### 🟠 ~~右腕:永久只有 DW2,`right_wrist` 通道从此是黑帧~~(08-12 被上面那条取代)
>
> 操作者 18:00 定案:「**右手算了就不连接了以后也不用,右手已经有双目了**」
> ——「双目」指的就是 DaBai DW2(两颗 IR 镜头 + 投射器,肉眼像双目)。
> 另外确认 DW2 **就是 08-10 那颗**,只是被挪到了右腕(`1-3.2` 口从 08-10 就是它)。
>
> **所以 udev 那条"两个口都映射到 `l_arm_cam`"的规则现在是永久的,我没有改回按口区分。**
>
> ⚠️ 但 yml 里的 `right_wrist` 节点**只要起图就会去开** `/dev/r_arm_cam`,而 `webcam_node.py`
> **打不开设备时不退出** —— 它填一张全黑 640×480 图、写上 "No Webcam was found",
> **照样 `send_output`**。表现不是报错而是**黑帧被当正常图像录进数据集**,
> 而且实测**持续烧 ≈12% 一个核**(6 s 涨 71 ticks)在生产垃圾。
>
> **要不要现在删这个节点,是个需要你拍板的取舍:** 恒黑通道**不像之前的左右对调那样有毒**
> (它只是白占编码器容量和磁盘),但它会**焊进数据集 schema** —— 先录 N 条 episode
> 再删节点,前后两批数据不兼容。**要删就趁早**,改 3 处:`right_wrist` 节点本体
> + 它在 `robot_daemon` 和 `dataset_recorder` 的两处 `inputs` 映射
> (`astrabot_data_collection_xr1_evt2.yml` 的 143/170/236 行附近)。
> 我**没有动这个 yml** —— 删厂商采集图的节点超出了"重启 dora"的范围,
> 而且它正在你刚切过去的那条数据链上。

> **🔀 路线已切换到模仿学习(LeRobot + ACT),开环感知链停工。见文末 §7 和
> `NEXT_SESSION_PROMPT.md`。** 下面 1~6 节是切换前的现状快照,仍然有效(设备状态、
> 恢复命令、桌高真值都还是权威),但"下一步做什么"以 §7 为准。

> ## 🔴 读之前先知道三件事(16:40 更新)
>
> 1. **桌高 = `0.8108 m`**。卷尺的 0.750、触桌反推的 0.7415 **全部作废**(§45)。
>    唯一权威是 `grasp_block.TABLE_TOP`;文档里凡是 0.75 那一代的数字都已挂作废框。
> 2. **`results/` 三份(能力图/正前方分析/工作区规格)的数字全部失效** —— 它们是在
>    桌高 0.75 上算的,重跑命令写在 `results/workspace_spec.md` 开头。**在重跑之前
>    不要拿它们决定推不推桌子、挪不挪底盘**。
>    🔴 **但这一条原来的结尾"『够不着、必须推桌』大半是桌高错的假象"是错的,
>    2026-08-12 12:40 已被实测推翻** —— 桌上积木在 x=0.745~0.881,窗口 x∈(0.18,0.56),
>    真的够不着,**桌子必须推**。桌高订正只把射线交点挪了 54mm,解释不了 300mm 的缺口;
>    那句话把"手臂当时停在 x=0.4749"错当成了"桌子在 x=0.4749",而**白桌带脚轮、
>    位置不是常量**。详见本文件顶部 12:45 一节。
>    教训:**订正一个量的时候,别顺手把靠另一个量得出的结论也一起注销掉。**
> 3. **摇操真值录制器正在跑**(`scripts/teleop_truth.py`,只读零 publisher)。
>    它不抢控制权、不碰录制器,但**它打开了 `chest` 和 `l_wrist` 两路 UVC** ——
>    要用这两个相机的话先看它在不在。
>
> ## 🟢 16:55 追加:第一条**验证过的**夹住样本到手了
>
> | | |
> |---|---|
> | 样本 | 右臂,指尖中点 **(+0.3925, −0.3734, +0.8608)**,夹爪 **124~125** |
> | 凭什么信 | 指令打满 1.0,读数在 124~125 上**稳了 365 秒以上**。标尺:**空爪合到底 6~7 · 夹住 40mm 黄积木 124~125 · 全开 838~840** |
> | ⚠️ 但是 | z=0.8608 **高出桌面 50 mm** —— 积木被**举在空中**。所以它是**手位置**样本,**不重新测桌高**。`TABLE_TOP=0.8108` 仍只靠那条夹爪读数 839 的样本,还带 ±10 mm |
> | 还缺什么 | **在桌面上**夹住的样本(消掉桌高残差)+ **x/y 铺开 4 条以上**(才解得出相机→base 的旋转,一条只锚一个标量) |
> | 判据又漏一次(已修) | 摇操扳机是**连续量**(0.0/0.129/0.6266/1.0),扣一半时落了条 `读数 348` 的假样本。现在要求 `cmd>=0.9` + 稳 **1.5 s**(真的那条稳 365 s,代价为零)。详见 §47 |
> | 录制器崩过一次(已修) | UVC `retrieve()` 拿到空缓冲区,cv2 在 `imdecode_` 里**抛异常**(不是返回 False),把只读录制器整个带走 → run `20260811-04` 判 crashed。相机是辅助通道,现已整段兜住 |
> | 相机侧 | 胸前相机**看得见被夹着的积木和手**;腕相机画面里**自己那两片橙色夹垫就在框内** ⇒ "积木有没有对准指间"可以**纯像素**判,不需要外参 |
>
> ## ✅ 17:35 更正:上面那条 17:05 的"左腕相机掉线"**结论是反的,已作废**
>
> - **左腕相机是活的,而且刚重装过。** 现在插在 hub 口 **4.3**,`/dev/l_arm_cam → video2`,
>   高速枚举干净(480 Mbit/s、无 `error -32`),抓帧实测 **1280×720**、亮度 143,没人占着它。
> - **装回去的位姿可复现**:拔插一整轮后再抓帧,画面上沿两片橙色夹垫的面积和质心
>   几乎逐像素重合(13289→13364 px @ (1049.8,60.8)→(1049.6,61.2);
>   10679→10701 px @ (381.8,54.5)→(381.7,54.5))⇒ **这次拔插不作废已标的腕部外参**。
> - 16:38 掉下总线的是 **4.3 口上原来那台 = 右腕**单目 —— 它是**为装 DaBai DW2 而拆的**,
>   不会回来了。**别再找 `/dev/r_arm_cam`。**
> - **4.3 口那 4 次掉线不是接触不良**:每次 disconnect 后面隔 30~43 分钟才重新枚举
>   (手在拔插的节奏),全程零条 `uvcvideo` 流错误。唯一那次 `error -32` 是一次插入
>   过程中的瞬态,自己升到 high-speed 了。别拿掉线次数当线材受损的证据。
>
> ### 🔴 "左/右"是插线的属性,不是规则的属性 —— 这条对模仿学习是硬约束
>
> 两台 DECXIN 同型号同 VID:PID 且 `ID_SERIAL` **完全相同**,udev 只能靠 hub 口号。
> 17:15 拔线实验证明当时接线与厂商规则**相反**;17:29 操作者改插到 4.3,规则**又对了**。
> 所以判断当下只有两招:**让人拔一根线看内核掉哪个口**,或**抓一帧看有没有同侧橙色夹垫**。
> 已装 `/etc/udev/rules.d/99-astrabot-wrist-camera.rules`(同名整体覆盖 `/usr/lib` 那份,
> 抗厂商重装),两个口都映射到 `l_arm_cam`,插哪个口都认。详见 `PITFALLS.md` §48。
>
> **后果**:dora 里 `left_wrist` 开 `l_arm_cam`、`right_wrist` 开 `r_arm_cam`,所以在
> 17:15 之前那段接线下,**两个腕部通道的左右是对调的** —— 已录的 episode 腕部图像标签是错的,
> **拿它们训 ACT 等于训在错标数据上**。§28/§44 那个"两份 URDF 给左右腕相机写了完全相同
> origin"的悬案同源,**修好之前拟合的腕部外参全是废的**。
>
> ### 这两件事的现状(原"还没做的两件事")
>
> - ✅ **dora 图已重启过一轮,现在整个停掉了。** 那两个 `(deleted)` 死 fd 随进程一起没了。
>   `right_wrist` 节点**仍在 yml 里**、仍指向不存在的 `/dev/r_arm_cam` —— 删不删见上面
>   18:15 那节(需要拍板)。重启配方见 §51。
> - 🟠 **DW2 装在右腕已由操作者口述定案两次**(「同一颗,我把它挪到右手了」/「右手已经有
>   双目了」)—— 是**证词不是仪器测量**;我那个相关性实验 300 s 里手臂没动过,判定不了,
>   已退役。它是 libusb 厂商类设备,**没有 `/dev/video*`**,只能走 `/camera/depth/image_raw`,
>   640×400@15Hz,**只有 depth+IR 没有 RGB**,起节点必须显式 `depth_height:=400`
>   (厂商 launch 默认 480 是无效值)+ `publish_tf:=false`。
>   ⚠️ **最小工作距离仍然没测**,但「上界 ≤302 mm」这个数是错的,已作废:302/303 只是
>   `experiments/dw2_minrange/sweep2.jsonl` **某一帧**的 `f_min`。整段数据(2700 帧 /180 s
>   与 2246 帧 /150 s)里 `f_min` 低到 **121 mm / 137 mm**,所以上界其实 ≈ **130 mm** 量级。
>   为什么仍然算「没测」:`f_min` 是全图**单像素最小值**,近距结构光的边缘噪点就能造出它,
>   而且那两段 sweep 是为「装在哪」拍的,场景没有卷尺真值配对 —— 没有「已知距离 → 深度
>   有效/失效」的成对数据,就不算测过。没测之前别把 DW2 当抓取闭环的深度源。
>   **这是原始请求里唯一还欠的量。**

> 这个文件是**会过期**的那一份。硬结论在 [`PITFALLS.md`](PITFALLS.md),不变量在
> skill `xr1-robot`,原理在 `docs/`。本文件只回答三件事:**现在机器是什么状态**、
> **卡在哪**、**下一步做什么**。
>
> ⚠️ RTC 是死的(§20),文件时间戳不可信。锚点用 `/proc/uptime`:写这份时
> **up 18 h 16 min**(即约 08-10 16:20 开机),`Astrabot_Controller` 于本日 10:01 重启过。

---

## 0. 一段话

**感知通了、控制通了、~~录制通了~~,而抓取一次都没成功过。** 38 次成形试验,`grasped` **0**。
(「录制通了」08-12 划掉:那批素材连 run 目录一起没了,08-11 之后 `index.jsonl` 全部 5 条
都是 `"video": false` —— 见 §「外置录像」和 `README.md` 里同一处的复核注。)
今早修掉四个彼此独立的遗留故障之后跑的第一次真尝试(`20260811-03`)证明了瓶颈**不在**
感知也**不在** IK:黄块认得准(x=+0.476, y=−0.155)、pre 和 grasp 两个 IK 都有解,
**卡在路径规划** —— 上一轮摇操把右手丢在 tcp z=0.547(比桌面低 20 cm),从那个位形去桌面
上方的关节空间直线一定扫过桌面,于是规划全判碰撞,机器人**正确地**一动没动(§43)。

方向因此换了一次:不再继续用卷尺往系统里注入绝对真值,而是**用摇操示范直接给出
「相机观测 → 关节指令」的带标签样本对** —— 判据从"停稳 2 秒"换成"**夹住了**",
因为积木被夹在两指之间的那一刻,两指中点**就是**积木的真实位置,不需要瞄准、不依赖
任何外参、而且自证(§41 末尾、§44)。

### 更新 — 同日傍晚:真值拿到了,38 次 0 成功有答案了

用户直接把右臂摇到「这个位置能夹住黄色积木」,`scripts/truth_now.py`(只读、零 publisher)
读下来:**两指中点 = (+0.4749, −0.3591, +0.8238)**,抖动 0.00 mrad。
`scripts/verify_fk.py` 用 rsp/KDL 独立复算,与 pytorch_kinematics 差 **0.00 mm**。

⇒ **桌面 = 0.8238 − 13 mm = 0.8108**,而代码里用的是 `TABLE_TOP = 0.7415`。
**每一次抓取都在瞄桌面以下 56.5 mm。** "IK 有解但规划全拒"不是规划器保守,
是目标点在桌板内部。这也解释了 §43 那次:手臂低于桌面固然是问题,但**目标本身**
也在桌板里。

连带三件事:
- **卷尺 0.750 过期**(低出真值带 44~64 mm),而 **ZED 拟合 0.8128 只差 2.0 mm**。
  §41 那个"+62.8 mm 偏差、分不清是深度偏还是卷尺过期"—— 是卷尺过期。
- **08-10 触桌探针的结论要反过来读**:它把「关节偏差出现」当成「接触发生」,
  而指尖到不了桌面下 70 mm,所以 0.790 处那个 1.03e-2 是手臂顶在桌面上的伺服误差。
  顶住之后偏差只会继续涨、不会消失 ⇒ **单调探针必须配一个独立量才能定标**。
- **`tcp_z()` 不能当碰撞地板**:它走 TF 只取平移、丢掉旋转,而 `TIP_CENTER` 有
  −22.5 mm 横向分量,歪斜时会转进 z。这个能夹住的位姿 `tcp_z=0.8417`、
  指尖中点 `0.8238`,只差 **17.8 mm 而不是 48.5 mm**;旧的 `TCP_Z_FLOOR` 会把
  这个**正确**位姿拦死。已改 `TIP_Z_FLOOR` + 新增 `XR1.tip_center(side, offset)`。

---

## 1. 子系统状态(附支撑它的实测数)

| 子系统 | 状态 | 证据 / 备注 |
|---|---|---|
| 手臂 | ✅ | 今日 10:01 重启控制器后 `/joint_states` **199.885 Hz**;三个控制器 active |
| 颈部 | ✅ | pitch **+39°** / yaw **0**(yaw 必须钉死在 0,§33) |
| 双 G2 夹爪 | ✅ | 今早 `kill -9` 掉攥着废 fd 的旧驱动后重拉:左 **816 mm** / 右 **829 mm**(§39) |
| ZED | ✅ | `Astrabot_ZED` **和 `Astrabot_ZED_Points` 现在都 active**(08-12 15:56 起,之前那句「_ZED_Points 仍 failed」已过期),static frame 共 23、**6 个 zed frame** 齐。验收要**数 frame 个数**,不是看图像 Hz(§38),而且不能用 `topic echo --once`,见顶部 16:05 一节 |
| 两路 UVC | ✅ 空着 | dora 已全停,`/dev/f_chest_cam`(胸前)+ `/dev/l_arm_cam`(左腕)都没人占。**只有两路** —— 右腕单目已拆。右腕现在是 **RealSense D455**(不是 DW2,DW2 已判定换掉),走 realsense2_camera 不占 `f_/l_arm_cam` |
| 外置录制器(Mac) | ✅ | 授权在(cdhash 未变)、时钟差 −1 s;**独占**,抢它会作废别人的试验(§29) |
| 摇操(Quest→LiveKit→dora) | 🔴 **已拆**(08-12 15:56 收摊) | ⚠️ 这里原写「🟢 正在运行(08-11 10:39 起),两个静默故障都预先堵住了」—— 那句的依据只有「4 路 track 全推、通道名对上」,**没有一条记录证明手臂动过**,已订正为未验证断言,见顶部 16:05 一节。08-12 重新拉起过一次:机器人侧全绿、手臂仍不动,真因在 Quest App 侧(载荷是字面 `hello world`),判据见 `docs/06` §5.1.1 |
| Nav2 3D 避障 | ✅ 有输入 | `Astrabot_ZED_Points` 已 active(08-12 15:56 起);只有在 dora 图占着 ZED 期间才缺点云 |

**摇操结束后的恢复动作**(会被忘掉,写在这里):

```bash
sudo -A systemctl start Astrabot_ZED.service Astrabot_ZED_Points.service
# 45 s 后验收:数 zed frame,必须 >=5(正常 6);光看 rgb Hz 会骗你(§38)
# ⚠️ 别用 `ros2 topic echo /tf_static --once` 数(只得到 2,会误报成 §38 故障),
#    要用 TRANSIENT_LOCAL QoS 订阅 /tf_static 转 5~6 秒
```

`Astrabot_ZED_Points` 被摇操拉起时 `kill -9` 会显示 **failed (Result: signal)** ——
那是正常结果,不是新故障,`start` 一下就回来(08-12 已复验)。

---

## 2. 今天(08-11 上午)发生了什么

**修掉四个彼此独立的遗留故障**,全部是前一晚留下的,四条修法互不相通:

| 症状 | 真因 | 修法 |
|---|---|---|
| `no /joint_states within 8s` | `joint_state_broadcaster` 哑了(`/dynamic_joint_states` 一起停) | 重启 `Astrabot_Controller` → 199.9 Hz。**注意重启后第一次短探针必然假阴性**(§40) |
| 夹爪读数静默 | 设备节点 **00:04 被重新枚举**,旧 `g2_gripper_node` 攥着失效 fd,且 SIGTERM 无效 | `kill -9` 后 `bringup`(§39) |
| 感知报拿不到 TF | **ZED 自己的 5 个 frame 一个都没发**,而机体 TF 完好、`tf2_echo base_link zed_camera_link` 会误报正常 | 重启 `Astrabot_ZED` → 42 帧 / 6 个 zed frame(§38) |
| 三路 UVC 打不开 | 前一晚摇操栈没收掉,V4L2 独占 | 按 `docs/06` 停图(`pkill -f` 自杀坑又踩了一次,退出码 144) |

**这四条的共同代价**:ZED 那条从**前一天 16:22** 就存在,期间 `xr1.py status` 一直全绿 ——
因为它查的是图像话题而**不数 TF frame**。已修进 `xr1_verify.py`(zed frame <5 直接 FAIL)。

**数据侧**:46 个 run(5.2 G)先挪进 `experiments/_attic_20260811/`,**08-11 傍晚经用户
明确指示已 `rm -rf` 删除**(空闲 391 G → 396 G)。删的理由不是省空间,是别让过期数据
继续污染判断:那 38 次里一次成功都没有,而且现在已知它们**全部瞄在桌面以下 56.5 mm**
(§45),对学习没有任何残值。`experiments/` 现在只有 08-11 的 run + `truth_samples.jsonl`。

**工具侧**:
- `plan_failed` 现在落盘 `q_pre`/`q_grasp` + 归因诊断(第几个路点、撞桌面还是贴躯干、
  深多少毫米)。之前 6.5 分钟算出的 IK **算完就扔**。
- `xr1_verify.py` 新增 TF 段(frame 总数 / 根数 / zed frame 数)。
- 两个只读探针:`depth_now.py`(覆盖率)、`plane_now.py`(精度)。

---

## 3. 试验账本(挪进 attic 前的最终统计)

| 裁定 | 次数 | 含义 |
|---|---|---|
| `grasped` | **0** | 一次都没抓到 |
| `held` | 13 | 夹爪合上了但没有形成完整 episode |
| `skipped` | 13 | 前置条件不满足,机器人正确地没动(今早那次是路径规划拒动) |
| `crashed` | 6 | 脚本异常(仍落盘) |
| `refused` | 5 | 安全守卫拒动 |
| `empty` | 1 | 合上但空爪 |
| 合计 | **38** | 目录 46 个(含非 episode 的探测 run) |

最后一次:`20260811-03`「恢复后首次真尝试」,437.5 s,**有录像 12271 帧**,裁定 `skipped`
—— 也就是**6.9 分钟的静止画面**。录像本身证明了链路是通的,只是画面里没有动作。

> 结论的方向不是"再多跑几次":38 次 0 成功、而失败原因每次都不同,说明缺的是**一次
> 可信的标定**,不是**更多的尝试**。

---

## 4. 卡点(按杠杆从高到低)

| # | 卡点 | 现状 | 为什么它排这个位置 |
|---|---|---|---|
| 1 | **手眼误差一次没量到** | `docs/10` §9 #9,4 次试验全中途中止 | 整条链上唯一**从未被测量**的量。没有它,下面几条都只能猜 |
| 2 | **`head_yaw` 转 40° → 同一块没动过的积木被算到 59 cm 之外** | 两次复现一致到 8 mm,**机制未定案**(§33) | 590 mm 是误差预算里最大的一项,比指尖修正大一个数量级。**当前对策:yaw 钉死 0** |
| 3 | **腕相机 origin 左右完全相同**,而关节轴是镜像的(§44) | 新发现,未定案 | 腕相机是唯一绕开 head_yaw 误差链的通道,却建在一个可疑外参上 |
| ~~4~~ | ~~**桌高:深度偏 +63 mm 还是 `TABLE_Z=0.75` 过期**~~ | **已解决 08-11 傍晚**:卷尺过期,桌面 **0.8108**,ZED 只差 2.0 mm | 摇操真值把它一次锚死。原以为它不重要,结果它就是 38 次 0 成功的直接原因 |
| 5 | **自主脚本必须先归零** | 已有修法(`xr1.py home`) | 不归零则"可达性"不可复现(§43),这也是 `docs/10` §9 #7 的机制 |
| 6 | **重置是自进化循环的真天花板** | `docs/09` 的老结论,未变 | 30% 成功率下人力 18.5 h > 机器人 13.9 h。**在 #1 解决前不进入循环** |

---

## 5. 下一步

**正在进行**(另一个会话,10:40 起):`scripts/teleop_truth.py` —— 摇操真值录制器。
和旧的 `teleop_record.py` 四处本质不同:

1. 判据 = **夹住了**(夹爪合拢 + 位置稳住 + 读数明显没到底),不是"停稳 2 秒"。
   把"人的目测瞄准精度"从误差链里彻底删掉。
2. **两条臂同时盯** —— 08-10 的教训是记录器只盯 `--side right` 而人摇的是左臂,
   录了 281 帧同一个姿态,一小时后才发现。
3. **存原始射线**而不只存 `err_mm`,于是误差可以分解成「沿射线」(桌高假设错多少,
   一个数就能修)和「垂直射线」(指向/外参错多少,必须真标定)—— 这个分解直接回答
   §33 那 590 mm 是平面问题还是指向问题。
4. **同时抓两个腕相机 + 胸前相机并存下腕相机 TF**,顺带把 §44 那个可疑外参拟合出来。

它**只读、一个 publisher 都不建**,所以不会和摇操抢控制权(`astra_arm.Robot` 一构造就
建手臂指令发布者,因此它不用 `Robot`,只用纯订阅节点 + 纯 FK)。

**之后**:拿到 ≥N 组「夹住时刻的两指中点 + 同一时刻的相机像素」→ 拟合映射 →
用映射替掉现在这条「颜色 blob + 射线投射 + 固定 `TABLE_Z`」的开环链 → 再谈成功率与循环。

### 5b. 真值已到手之后,剩下的那一半(08-11 傍晚)

已改完的常量(`grasp_block.py`,下游脚本全走 `G.*` 所以自动跟随):

| 常量 | 旧 | 新 |
|---|---|---|
| `TABLE_TOP` | 0.7415 | **0.8108**(= 真值 0.8238 − 13 mm) |
| `TABLE_Z` | 0.750 | **0.8108**(感知与运动统一,旧代码里差 8.5 mm) |
| `GRASP_TIP_Z` | 0.7545 | **0.8238**(实测,不是推的) |
| 感知射线打的平面 | 0.790 | **0.8508** |
| 运行时地板 | `TCP_Z_FLOOR`=裸 tcp_z | **`TIP_Z_FLOOR`=0.8188**,读指尖中点 |

`plane_now.py` / `head_fov_check.py` 里各自硬编码的 0.75 也一并改了。
`head_fov_check` 重算后 **`pitch=+40°` 仍然最优**(真值点落在可见带 0.229~1.88 m、
水平半宽 0.48 m 内),那条结论不变。

**下一步(需要 ZED,所以必须等摇操停)**:真值已落盘 `experiments/truth_samples.jsonl`,
**积木不会动**,所以顺序是 —— 停摇操 → 起 `Astrabot_ZED.service`(数 zed frame ≥5)→
对同一块黄积木取一帧 → 与 (0.4749, −0.3591, 0.8238) 比 → 误差拆**沿射线**/**垂直射线**
(`teleop_truth.py` 里的 `decompose()` 是与数据源无关的纯函数,直接复用)。

⚠️ **一个必须验、但现在还不能验的可疑对比**:`20260811-03` 当时感知报黄块
x=+0.476 / y=−0.155;真值是 x=+0.4749 / y=−0.3591。**x 差 1.1 mm,y 差 204 mm。**
但那是用**旧平面 0.790** 打出来的,而且**不能确定积木这中间没被挪过**(用户一直在摇操),
所以这个"x 神准、y 错 20 cm"的不对称**目前只是个假设**。若积木确实没动过,
这个不对称会很有信息量:平面高度错只会沿射线整体推,推不出"一轴准一轴差 20 cm"
⇒ 指向/外参错,而且集中在一个方向。**取到新帧后第一件事就是复核它。**

顺带一个不用相机的推算:把历史报的 x≈0.55 那条射线原封不动重打新平面(0.8508),
x 落到 0.496,对真值误差从 **+75 mm → +21 mm**。所以"积木超出可达带 0.22~0.46、
必须先挪底盘"大半是桌高错造成的假象 —— 手臂此刻实际就停在 x=0.4749。
桌高这处订正**不吸收** §33 那 590 mm:转角误差的位置效应随距离放大,与平面高度无关。

---

## 6. 现在动手前必须知道的三件事

1. ~~**摇操在跑**(08-11 10:39 起)~~ —— **已过期**:08-12 15:56 起 dora 图全拆、ZED 两个服务
   都 active、UVC 全空。下面这条约束**只在 dora 图真的在跑时**成立(先 `pgrep -af dora`
   确认,别照抄):图在跑时**别去 `systemctl start Astrabot_ZED`** —— 会把 `/dev/video0`
   抢回来拖垮 dora 整张图(`docs/06` §2.4:dora 里任何节点退出会拖垮全图)。
   反过来,**采真值的图可以直接把 `eye_zed` 节点从 yml 删掉**,ZED 就不用停,见
   `docs/06` §5.1.1。
2. **录制器独占**。开录前 `python3 xr1_cam.py status | grep -E '"(state|clip)"'`;别人占着就
   等,**不要 `xr1_cam.py stop`**(会作废别人整个试验,§29)。
3. **同一时间可能有多个会话在动这台机器**。写这份文件时有两个会话在跑(一个摇操、一个
   写录制器)。重启服务、抢设备、`pkill` 之前先 `pgrep -af` 看一眼是不是别人的进程 ——
   重启是外溢动作,§40 元教训 30。

---

## 7. 🔀 17:20 路线切换:下一步走模仿学习,开环感知链停工

**开环链已停。** 38 次试验 0 成功,卡点是手眼标定这一次都没量到的量(§33 那 590 mm
也在里面)。真值录制器拿到了第一条可信样本(见上面 🟢 16:55),但要把外参解出来还需要
≥4~6 条在 x/y 上铺开的样本 —— 那是另一天的工作量。

**新路线:LeRobot + ACT 模仿学习。** 决定性发现是**厂商 dora 栈本来就在写
LeRobot dataset v2.0**,不需要写任何格式转换:

| | |
|---|---|
| 配置 | `/home/astrabot/deploy/.astra/astrabot_data_collection_xr1_evt2.yml` |
| 数据根 | `/home/astrabot/deploy/.astra/data/dataset_<时间戳>/` |
| 格式 | `codebase_version: v2.0`,`fps: 30`,5 路视频 + 双臂 7 关节 + ee_pose[9] + 夹爪 + `action_{left,right}[10]` |
| 录制触发 | `record_toggle: teleop/Button_A`(`teleop` 节点 outputs 里**确实**声明了 `Button_A`,链路是通的) |
| 战绩 | **7 个 dataset 目录,parquet=0 / mp4=0 / total_episodes=0 —— 一条都没录下来**(17:20 复查仍是 0) |

ACT 学 `像素+关节角 → 关节指令`,**不需要**手眼标定、`head_yaw` 误差、桌高常量、
射线投射、颜色阈值 —— 也就是绕开了目前全部卡点。

**完整的下一会话 prompt 已落盘:`NEXT_SESSION_PROMPT.md`**(6 个 Gate,每关带硬判据)。
其中两条是这次新识别的风险,记在这里免得丢:

1. **Gate 2 是唯一会让 50 条数据全部作废的一关**:如果录进去的 `action` 其实是**测量到
   的当前关节角**而不是**下发的指令**,策略只会预测"我已经在的地方",机器人一动不动,
   而训练 loss 低得非常好看。判据:`action[t] ≈ state[t+k]` 的残差必须在某个 `k>=1`
   取最小,而不是 `k=0`。**这是静默失效,同 §47 一个族。**
2. **训练环境是空的**:厂商 venv 里 `torch 2.10.0+cpu`(`cuda.is_available()` 为 False),
   `lerobot` 没装。机器是 Thor / JetPack **7.0-b128** / L4T **R38.2.2**,`nvcc` 不在
   PATH 上。这是 2025-09 的新平台,`pip install torch` 一定给 CPU 版。
   **别装进厂商 venv**(dora 在用,弄坏了摇操就没了),新建独立 venv。

时间口径说清楚:**1 小时做不完全程**,ACT 训练本身就是几小时量级。1 小时能干完的是
唯一需要人的那部分 —— 录 50 条已验证的示范 + 备好训练环境,训练之后无人值守跑。

---

## 8. 🔁 17:55 排序又变了:经典路线现在比 ACT 更近

上面 §7 说"切换到模仿学习"。**17:50 的实测让这个排序反过来了**,理由是两边的缺口不对称:

| 路线 | 还缺什么 | 缺口性质 |
|---|---|---|
| **ACT** | ① episode **一条都没录出来过**(0/7 会话,17:20 起主动监视 20 min 仍 0)<br>② CUDA torch 在 Thor/JetPack 7.0-b128 上**没装**<br>③ **已录数据的腕部左右标签是错的**(17:35 那条) | **三个未建成的前提**,其中 ① 连原因都还没定位 |
| **经典(建模+规划)** | 手眼外参 —— 一个测量 | **一个缺口,而且测法刚刚变成自动的** |

**决定性的两条新事实:**

1. **DW2 的 `camera_depth_optical_frame` 不在 TF 的 39 个 frame 里**(§49)。它 14.9 Hz、
   640×400、90.3% 有效像素全都是真的,但**图里没有这条边**,像素变不成机器人坐标。
   ⚠️ `grep` 会命中 `chassis_left_camera_depth_optical_frame`(底盘那台,frame 是齐的),
   必须用 `tf2_echo` 判。
2. **积木还夹在手里 ⇒ 手眼标定可自动化。** 手里夹着的物体位置由 FK 已知,而 ZED 和 DW2
   看的是**同一个刚体**。手臂自己摆 5~6 个位姿,同时记 `(FK 指尖中点, ZED 像素+深度,
   DW2 深度)`,**一次解出两台相机的外参** —— 不再需要人反复瞄准。

**加传感器解决不了的四条(别再往这上面花时间找硬件方案):**

- **ZED 看不到自己的工作区**:`head_pitch` 必须顶在 +40° 极限,能居中的角度超出关节极限
- **`effort` 全 `.nan`**,没有力/触觉 ⇒ "碰到桌面了没有"感知不到,模型误差没东西兜
- ~~**右腕 RGB 已被拆掉装 DW2**,不会回来 ⇒ 抓取臂有深度没彩色~~
  **08-12 作废**:右腕换成 D455,彩色+对齐深度都有了(见上面 13:20 那节)
- **规划器会拒动**(§43),与感知无关的独立卡点

**这条风险 08-12 在 D455 上量到了(DW2 上仍然没量)**:抓取时积木离腕部 100~300 mm,
而 D455 的对齐深度 **`<250 mm` 一个像素都没有**、最近 295 mm ⇒ **合拢那一刻确实是瞎的**。
所以"腕部深度闭环到手指"这条路对 D455 也不成立,除非换 D405(min-Z ~7 cm)。
腕部相机能干的是**中程对准**(把手引到积木上方 30 cm 以内),最后一段只能靠开环 + 几何。
~~DW2:整帧最近像素 834 mm,若最小工作距离≈800 mm 则抓取瞬间是瞎的~~ ——
DW2 那个 834 mm 是**它当时看的场景**没有更近的东西,不是它的极限;两次逼近实验都没触发失效。

### 下一步的最短链(全部需要摇操先停)

```
停摇操 → 起 ZED 两服务、数满 6 个 zed frame → xr1.py home
      → 摆 5~6 个位姿,同时解 ZED 和 DW2 外参 → 一次建模 → 抓
```

~~顺带必须处理:dora 里 `right_wrist` 节点**永远打不开设备了**~~
**✅ 2026-08-12 已做**:`right_wrist` 节点连它的两条输入映射(`robot_daemon` 和 `data_buffer`
里的 `observation_images_right_wrist`)已从 `/home/astrabot/deploy/.astra/astrabot_data_collection_xr1_evt2.yml`
删除,原处留了注释;备份 `...yml.bak-20260812`。校验:节点 10 → **9**,悬空输入 **无**。
⚠️ 现在右腕换成了 D455(有彩色也有深度),要不要把右腕通道**接回去**、以及接哪一路
(`color/image_raw` 还是对齐深度)是个**新决定**,没人拍板前不动 —— 一旦写进
`data_buffer` 就会进数据集 schema。
