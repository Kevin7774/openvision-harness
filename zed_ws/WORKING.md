# WORKING.md — 当前任务状态（易变，随时被覆盖）

**任务**：抓黄色积木 → 放进白碗（碗内已有一个红方块），至少两个完整 Cycle，
第二个 Cycle 必须重新观察、不复用旧绝对坐标。

**实验记录**：`experiments/harness/experiment_NN.md` + `experiment_NN_full.mov` + `keyframes_NN/`。
编号从 **07** 起（01/02/04/05/06 是今天更早会话录在 Mac 上的，本地无报告，见 §历史）。
已完成并归档：**07 / 08 / 09**（全部含视频＋关键帧；09 是失败实验，证据全留）。

## 现状（2026-08-17 16:30）

| 项 | 值 | 来源 |
|---|---|---|
| 右臂指尖中点 | (0.5775, −0.2059, **0.9305**) | FK on live `/joint_states` |
| 桌面 / 抓取高度 | 0.790 / 0.8001 | `grasp_block` 常量 |
| 右爪 | 839 = 全开 | `xr1.py pose` |
| 头 | pitch +39.0° yaw 0.0° | `servo.py` 自查 |
| 黄积木像素 | **(664.8, 282.5)**（钉死；10 次测量一致，四帧证明未移动） | 账本 + 原图放大 |
| 黄积木世界坐标 | **(0.5922, −0.1574, 0.830)**，source=`ray_plane`（无深度） | `G.perceive` 16:20 |
| 场景 | 白桌：白碗(左, 内含红方块) · 绿方块(中, world 0.616/0.182) · 黄积木(右) | ZED 原图 |
| 录像 | 已停；09 已拉回本地（3.18GB / 1479.8s） | `xr1_cam.py` |

## 卡点（**需要人**）

**H-reach**：积木在 x≈0.59，超出右臂在抓取高度 z=0.8001 的可达范围。
两条独立证据：
1. 今天 `plan_descent.py --z 0.8001` → `REFUSED: no safe IK`（x=0.5775, y=−0.157）。
2. 08-14 记下的决定性一对：**x=0.589 抓取高度无解 / x=0.480 有解 0.00mm**。

正在跑 x = 0.592/0.560/0.530/0.500/0.470 的 IK 扫描定边界（只读、不动机器人；
无解的 x 走穷尽搜索，很慢）。

需要的物理动作（二选一，都要人）：
- **(a) 把黄积木往机器人近侧推 ~10cm**（推荐：白桌带脚轮但桌高真值不变，改动最小）；
- **(b) 授权把底盘往前开 ~10cm**（会让所有已量的世界系数字作废，代价大得多）。

⚠️ 不是软件问题。感知、伺服、闸门这一路今天都验证过是好的。

## 已验证可用（都进了 GUIDE）
- 方阵解伺服（du, dv, dz 一起解）dry-run 精确砍半 —— 但 `--go` 会被**夹爪体模型**否决
  （`夹爪体离桌面只有 −29mm（下限 +5）`）。这是安全判据，`CLAUDE.md` 要求人授权才能放宽，
  **没有放宽**。注意这跟 H-reach 是两个独立的墙。
- `--blob U,V` 钉死目标像素（治遮挡拖动质心）。
- 两次纯竖直下降残差都是 0.0mm。

## 下一步验证
1. 扫描结果 → 给出「推到 x=? 」的具体数字。
2. 人把积木推近 → **重新观察、重新定位**（Cycle 的前提，不是绕过它的借口）→
   `--retarget` 或人工核对原图后 `--blob` 重新给锚点 → 方阵解伺服下降 → 合爪 → 视觉确认。
3. 夹爪体模型那道墙：若推近后 `servo` 仍被它否决，而 `plan_descent` 能降到底，
   就用 `plan_descent` 走最后一段（它不检夹爪体，靠指尖地板＋碰撞），不去动模型。

## 历史（本地无报告，仅 Mac 侧视频）
`apple@192.168.123.138:/Users/apple/xr1rec/clips/`：`tmpjuqvdky3_experiment_01_full.mov`
`experiment_02_full.mov` `experiment_04_full.mov` `experiment_05_full.mov`
`experiment_06_full.mov`（03 缺号，非本会话造成）。今天更早的会话只留下
`vista_runs/yellow-exp0{1,2,4,5,6}-20260817/`（全是 `observe` 事件，**没有一次物理动作**）。
