[package]
name = "vision-harness"
version = "0.1.0"
edition = "2021"

[dependencies]
nalgebra = "0.32"
roxmltree = "0.19"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
lto = true
codegen-units = 1
